# Журнал работ

## Бэклог (технический)
- [ ] Pulse: подавление сравнения волн с почти одинаковой аудиторией (остаточный риск из ревью #24)

## Блокеры (нужен владелец)
- [ ] AI (#ai): ввести ключ проекта AI Broker (Інтеграції → AI Broker → «Ключ проєкту», режим «Демо»), включить «Дозволити AI», нажать «Тестовий запит»; лиду — прогнать `ai:experiment` по фикстурам и выбрать возможность на каждую функцию — [ai.md](modules/ai.md)
- [ ] Reports (#desk): гендерный разрыв в оплате — нужны данные о зарплате и поле (и правовое основание); пока отчёта нет — [reports.md](modules/reports.md)
- [ ] Подключить Google (почта/календарь/таблицы): «Інтеграції → Підключити Google» под ящиком-владельцем — согласие даёт только владелец
- [x] `GOOGLE_CLIENT_SECRET` в Vercel env — внесён (тестовый; перед продом перевыпустить: секрет прошёл через чат)
- [x] Постоянный `VERCEL_TOKEN` (`sinhrm-github-actions`, без срока) в GitHub secrets — перед продом перевыпустить (прошёл через чат)
- [ ] Google-интеграции (#goog): добавить redirect URI `https://sinhrm.vercel.app/api/google/connect/callback` в OAuth-клиент, включить Gmail/Calendar/Sheets API, добавить scopes на consent screen; в режиме Testing — «Перепідключити» раз в 7 дней; откалибровать парсеры писем на реальных (обезличенных) письмах — [google-workspace.md](modules/google-workspace.md), [mail-agent.md](modules/mail-agent.md)
- [ ] Каналы (#chan): токены и адреса вебхуков по шагам из [modules/channels.md](modules/channels.md) (Telegram: bot_token → «Зареєструвати вебхук» → бот в Telegram Business с правом отвечать; WhatsApp: access_token, app_secret, verify_token; Viber: token; телефония: webhook_token + пример реального вебхука, чтобы подтвердить формат).

Короткие записи: что сделано, где искать. Подробности — в PR.

| Дата | Что | PR |
|---|---|---|
| 2026-10-08 | AI (#ai, раунд 1 эксперимента): промпты v3 — калибровка уверенности почты и авто-правило только при ≥ 0,85 + конкретном признаке, must/nice и потолок 69 в скрининге, без вызова модели при пустых данных (`insufficient_data`, пустое письмо), правила «ссылка»/«призыв к действию» в оценке скрипта; возможность по умолчанию `chat:fast` | — |
| 2026-10-08 | AI (#ai): модуль Ai — AI Broker (`POST /v1/jobs?capability=…` + опрос `GET /v1/jobs/{id}`, `X-Project-Key` из vault, `OutboundUrlGuard`, без логирования промптов/ключа), `OpenRouterProvider` как альтернатива; `AiService`: ворота (общий флаг → ключ и режим интеграции → выключатель функции), дневные лимиты из `ai_requests` (200 запросов / $2, `ai_budget_exceeded`), опрос с бэкоффом ≤ 40 с → отложенный ответ и задача `ai.poll` (применение ровно один раз, истечение 24 ч), один повтор после невалидного JSON → `ai_invalid_output`; возможность брокера на каждую функцию (`chat:sales` по умолчанию, пишется в `ai_requests.capability`), модель по умолчанию пусто (брокер выбирает, поле `model` не отправляется); промпты — версионированные чистые классы на общем `PromptBuilder` (ROLE/TASK/RULES/OUTPUT, компактный JSON, стабильное спереду для кеша); оценка по скрипту ШІ (балл по весам на сервере, сначала правила → замена ответом ШІ), сортировка почты незнакомых отправителей (≥ 0,85 → правило `source=ai` + переразбор писем очереди, ниже — подсказка; фильтр «створені ШІ»; бэклог в `mail.sync`), ШІ-скринінг кандидата по ТЗ 6 (ручной из карточки, автоскрининг `ai.screen` выкл. по умолчанию, «Оцінка ШІ, рішення за людиною», без ФИО и контактов); панель «ШІ: стан, використання і перевірка» с тестовым запросом; `php artisan ai:experiment` + по 6 синтетических фикстур на функцию; живой брокер не вызывался | — |
| 2026-10-07 | Интеграция с Sintegrum удалена по решению владельца: импорт справочников (`POST /api/directory/import`), интеграция `sintegrum_api` и колонки `external_id` справочников удалены (миграции `2026_10_07_100001`, `2026_10_07_100002`); справочники ведутся вручную | #27 |
| 2026-10-06 | Рекрутинг и HR (#hrq, фаза 6): модуль HiringRequests (ТЗ 2) — заявка на подбор (филиал, отдел, должность, количество, новая позиция / замена сотрудника, дата выхода, вилка зарплаты, требования, приоритет), мастер из трёх шагов, настраиваемые поля формы (тип, «обовʼязкове», порядок), отдельные права на создание (админы, руководители, список) и согласование (шаг маршрута), маршрут руководитель → роль/пользователь (директор филиала) → HR со снимком при отправке и SLA в днях с флагами просрочки, задачи `hiring_approval` согласующим (один раз на шаг) и эскалация HR (`hiring.sla`), автосоздание вакансии после финального согласования под блокировкой строки (идемпотентно), привязка существующей вакансии, прогресс «нанято / нужно», автозакрытие; каналы привлечения (ТЗ 3) в Recruiting — справочник `acquisition_channels` с типами, правила UTM (точнее → приоритет → старше), расходы по периодам, `candidates.channel_id` + `added_via` (manual/import/mail/extension/webhook/sheets) на всех путях создания, data-миграция источников в каналы (повторяемая), поле API `source` сохранено, блок «Джерела відгуків» вакансии, отчёт `channel_effectiveness` (конверсия, расходы пропорционально дням, цена найма — только админам); модуль Time — табели по неделям с записями (проект/категория/заметка), график сотрудник → филиал → компания → 8 ч Пн–Пт, сверхурочные и недоработка, согласованные отпуска (полдня) и праздники как отсутствие, отправка / возврат / согласование руководителем поддерева, `time.reminders` в пятницу (раз в неделю, закрывается при отправке), команда, графики; отчёты `time_by_employee`, `time_by_department`, `time_overtime`, `time_missing`; блоки главной «hiring» и «time»; «Мої задачі»: источники `hiring`, `time` | — |
| 2026-10-05 | HR (#desk, фаза 5d): модуль Desk — обращения сотрудников в HR (категории с SLA первого ответа и решения, статусы new/in_progress/waiting/resolved/closed, ответственный — только HR, ветка с внутренними заметками, которых сотрудник не видит, ссылки на опубликованные статьи базы знаний, файлы ≤ 2 МБ по правилам Documents), флаги SLA считаются при чтении, задание `desk.sla` ставит задачу `desk_sla` раз на обращение и цель; модуль SafeSpeak — анонимные сообщения без пользователя/IP/UA/времени (только дата), код доступа 80 бит показывается один раз (в БД HMAC), анонимные маршруты без сессии, лимиты отправки и подбора кода по HMAC-хэшу адреса, входящие только у админов с флагом `users.safe_speak_handler` (кнопка в «Користувачі»); модуль Knowledge — статьи Markdown → очищенный HTML рендерером Documents, аудитория все/филиалы/роли, поиск ILIKE (SQLite — LIKE), теги, версии, «Корисно?»; модуль Assets — реестр с уникальными номерами, выдача/возврат с историей, вкладка профиля «Активи», действие воркфлоу `collect_assets` (исполнитель из модуля Assets через тег); модуль Reports — реестр `ReportDefinition` с 20 отчётами поверх существующих сервисов и скоупов (People/AccessibleBranches), возраст и PII — только админам, eNPS только по закрытым волнам с подавлением, настроение — завершённые недели; конструктор по белому списку колонок (422 на неизвестное), сохранённые отчёты, потоковый CSV с защитой от формул; отчёт о гендерном разрыве в оплате не сделан — нет данных о зарплатах и поле | — |
| 2026-10-04 | Ревью #24 (#perf): результаты анонимных волн — только после закрытия (до этого диапазон участия), руководителям — только после закрытия любой волны; разрезы по отделам/филиалам показываются, только если и отдел, и остальная часть волны ≥ минимума (`SafeSegments`), вопросы с малым числом ответов скрыты; `PUT /api/pulse/waves/{id}` — минимум только повышается; 360: группы коллег/подчинённых — только после закрытия цикла (до этого диапазон отправленных); настроение команды и уведомления — только по завершённым неделям | — |
| 2026-10-04 | HR (#perf, фаза 5c): модуль Perform — 1:1 (шаблоны повестки, общие заметки, **личные заметки видит только руководитель встречи**, договорённости), цели OKR (ключевые результаты, прогресс считает сервер, дерево выравнивания без циклов, видимость public/team/private, история check-in), KPI (план/факт по периоду), фидбек (благодарность/конструктив/запрос, видимость, ответ на запрос один раз), оценка 360 (шкалы, компетенции, мастер цикла, генерация форм self/manager/peer/upward, группы коллег и подчинённых скрыты при < 3 оценщиках и без имён, сотрудник видит результаты после закрытия), планы развития, вкладка профиля «Продуктивність»; модуль Pulse — конструктор опросов (шкалы, eNPS, варианты, текст; шаблоны eNPS/вовлечённость/адаптация/exit), волны once/weekly/monthly/quarterly с аудиторией, анонимность (HMAC-хэш с солью волны вместо id, соль стирается при закрытии, без времени ответа, группы < 5 скрыты, отдельных ответов нет), eNPS, сравнение волн по вопросам и отделам/филиалам (ТЗ 1), опросы 30/90 дней после найма и exit по `EmployeeTerminated` (идемпотентно), настроение дня на главной, тренд команды от 5 человек, уведомление руководителю задачей `mood_alert` раз в неделю (ТЗ 5; именного таймлайна нет — вопрос владельцу), `pulse.tick`; «Мої задачі»: источник `pulse` | — |
| 2026-10-03 | HR (#wf, фаза 5b): модуль Workflows — шаблоны онбординга/офбординга/довільні (шаги с днём относительно якоря и исполнителем employee/manager/hr_admin/specific_user, `config` проверяет исполнитель действия), снимок шагов при запуске, автозапуск по событиям People `EmployeeHired` / новому `EmployeeTerminated` и по окончании испытательного срока (идемпотентно на сотрудника и шаблон), ручной запуск из профиля, 10 исполнителей (Open/Closed, тег): задачи, форма, запрос документа, наставник, уведомление руководителю, документ из шаблона, вебхук (`OutboundUrlGuard`, HMAC-подпись ключом шаблона из `SecretVault`, 10 с), вложенный воркфлоу (глубина ≤ 3), письмо и событие Google (не подключено → `skipped`; Gmail только чтение → письма `send_not_supported`), выполнение `workflows.tick` через `/api/ops/jobs/run` с захватом шага, ошибки → `failed` + «Повторити»; модуль Documents — шаблоны с переменными, документы (Markdown → очищенный HTML на сервере), файлы ≤ 2 МБ в БД за `DocumentStorage` (тип по содержимому), «Надіслати на ознайомлення» → «Ознайомлений» (хэши IP/UA) / «Відхилити», «Мої документи»; КЕП — заготовка `kep_signing` в «Інтеграціях»; единые «Мої задачі» (`tasks.employee_id/link`, типы `workflow`/`document`, исполнитель-viewer может закрыть свою задачу, закрытие задачи закрывает шаг) | — |
| 2026-10-02 | HR (#hr, фаза 5a): модуль People — сотрудники (вход не обязателен), уровни видимости справочник / работа / личное через `PeopleScope` (админ = HR, руководитель — всё поддерево по `manager_id`), справочник с фильтрами, профиль с вкладками, оргструктура, увольнение, «Мій профіль» и запросы на изменение (белый список полей, согласует руководитель или админ), `POST /api/applications/{id}/hire` (идемпотентно, кнопка на доске и в карточке); модуль TimeOff — типы (отпуск 24 дня, больничный и за свой счёт без лимита), политики по филиалам (как настройки Sintegrum), праздники, журнал баланса, запросы (пн–пт минус праздники, полдня, без пересечений, проверка баланса, override админа), согласование → журнал, отмена → возврат, календарь команды, «Погодження», начисление `timeoff.accrue` (год наперёд пропорционально / помесячно, сгорание сверх переноса 1 января), блоки «Відсутні сьогодні» и «Мої погодження» на главной (`DashboardSection`); импорт сотрудников из Sintegrum не сделан (API неизвестен); по ревью: блокировка строки сотрудника против гонок пересечения/баланса, уволенные скрыты от посторонних, помесячное начисление без дрейфа округления, калькулятор дней бросает исключение на длинном периоде | — |
| 2026-10-01 | Расширение (#ext): Chrome MV3 «SinHRM Clipper» (`extension/`, TypeScript + esbuild, Vitest на вымышленных фикстурах) — по клику читает только открытую страницу профиля LinkedIn/Work.ua/Djinni/DOU (JSON-LD → `og:` → DOM), окно с правкой полей и вакансией, настройки (адрес API, токен), uk/ru/en, `npm run package` → zip, job `extension` в CI; API в Recruiting: токен Sanctum `clipper` (90 дней, один активный, только `/api/clipper/*` через `authenticateAccessTokensUsing`), `GET /api/clipper/me`, `POST /api/clipper/candidates` (дедуп по ссылке профиля `candidate_profile_urls` + контактам, 201/200/409, заметка «Imported from …», 30/мин, CORS только `chrome-extension://`); страница `/settings/extension`; docs-check знает папку `extension/`; риск ToS LinkedIn описан, селекторы не проверены на реальных страницах | — |
| 2026-09-30 | Каналы (#chan): вебхуки `POST /api/webhooks/{key}` для Telegram Business (секрет-заголовок, «Зареєструвати вебхук» → `setWebhook`), WhatsApp Cloud (HMAC `app_secret`, GET-handshake, окно 24 ч → `template_required`), Viber (HMAC, `set_webhook`), телефонии Phonet/Ringostat/Binotel (`?token=`, форматы временные) → один путь `TouchpointIngestor` (ветка разговора `meta.thread`, гонка дублей), отправка из карточки `POST /api/candidates/{id}/messages` («Записати вручну», если не подключено), click-to-call (Ringostat callback, временно), проверки соединения WhatsApp/Viber, блок «Вебхук» и демо-события на странице интеграций; живые провайдеры не вызывались | — |
| 2026-09-29 | Google (#goog): подключение Gmail/Calendar/Sheets OAuth-согласием отдельно от входа (токены в `SecretVault`, refresh по требованию, `invalid_grant` → `reconnect_required` + предупреждение на главной), почтовый агент (правила отправителей без AI, отклики → кандидат + заявка + задача «позвонить за час», письма кандидатов → касания, очередь незнакомых, идемпотентно по id Gmail; парсеры work.ua/robota.ua/Djinni/общий — не откалиброваны на реальных письмах), встречи из карточки (Calendar v3 + Meet), импорт из Google Sheets (сопоставление колонок, дедуп, инкрементально, `sheets.sync`); `CandidateService::createOrMatch`; docs-check в kebab-case | — |
| 2026-09-28 | Scripts (#scr): версионируемые скрипты (черновик → публикация → откат), оценка звонков/переписки по правилам (шаги с цитатами, «следующий шаг зафиксирован», рекомендации; AI-заглушка за `AiPolicy`, провайдеров не вызывает), шаблоны в карточке, задачи-напоминания через `POST /api/ops/jobs/run` + `cron.yml`, отчёт `/api/reports/scripts`; Overview: дашборд вместо страницы статуса (`/status` — админам); заголовки вкладок «SinHRM · …» | #18 |
| 2026-09-27 | Recruiting (фаза 1, ядро): воронки (по умолчанию 8 этапов), вакансии + доска drag&drop, кандидаты с дедупом по телефону/e-mail/Telegram, маршрут и лента касаний, «Вхідні», зависшие (`last_touch_at`), отчёты, `TouchpointIngestor`, `recruiting:demo` в preview-сиде, Ctrl/⌘+K | #17 |
| 2026-09-26 | Справочники (филиалы, города, отделы, должности), филиалы пользователей + `AccessibleBranches`, импорт из Sintegrum API (не проверен на живом API), трассировки без аргументов, preview-web → preview-API | #16 |
| 2026-09-26 | Интеграции: зашифрованное хранилище секретов, реестр интеграций, проверки (Telegram, AI Broker health), выключатель AI, админ-страница | #15 |
| 2026-09-26 | Гость на защищённом эндпоинте: 401 вместо 500 | #14 |
| 2026-09-26 | Вход через Google (только по приглашению), роли, админка пользователей, i18n uk/ru/en, оболочка с темой | #13 |
| 2026-09-26 | Деплой: обычный curl вместо vercel curl, защита preview снята (только синтетика) | #12 |
| 2026-09-26 | Ops: лимит только неудачных попыток (миграции работают на пустой БД), JSON-логи на Vercel | #11 |
| 2026-09-26 | Миграции через `/api/ops/migrate` (секрет), APP_ENV по окружениям | #10 |
| 2026-09-26 | Деплой: API в fra1, .vercelignore, DB по умолчанию pgsql, обход Neon SNI только для libpq<14, preview БД пересоздаётся | #9 |
| 2026-09-26 | Фаза 0: каркас Laravel 13 + Angular 22, модульная структура, health, CI, документация | #1 |
